namespace Enums
{

    public enum Trajectory
    {
        Target,
        Predicted
    }

    public enum DataType{
        Coordinate,
        Velocity
    }
    public enum BrowserEvent{
        Opened,
        LoadedTarget,
        LoadedPredicted
    }

    public enum GraphDisplay{
        X,
        Y,
        Z,
        XY,
        YZ,
        XZ,
        XYZ
    }
    
}
